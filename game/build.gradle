dependencies {
    compile project(':engine')
}

apply plugin: "application"

jar {
    manifest {
        attributes(
                'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
                'Main-Class': 'ch.redmonkeyass.game.ZombieInvasion'
        )
    }
}